require "spec_helper"

describe SimpleRole do
  it "has a version number" do
    expect(SimpleRole::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
